export type DeviceViewType = {
  ip: string;
  title: string;
  lastActiveDate: string;
  deviceId: string;
};

export type DeviceType = {
  id: string;
  ip: string;
  title: string;
  lastActiveDate: string;
  expirationDate: string;
  deviceId: string;
  userId: string;
  createdAt: string;
};
